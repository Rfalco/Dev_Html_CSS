
var cIniCor = '#000000'
var cFimCor = '#ffffff'
var cCorAtu = cIniCor

function animate() {
    requestAnimationFrame(animate)

if (cCorAtu === cFimCor) {
    cCorAtu = '#000000'
} else {

    l
    cCorAtu = '#000000'
}

    console.log(cCorAtu)
    // console.log(cFimCor)
}

animate()